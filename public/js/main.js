// Screen router. Grows a real menu in M1/M2; for M0 it proves module loading.
const app = document.getElementById('app');
app.innerHTML = '<p style="margin:auto;color:var(--dim)">8Ball — scaffolding (M0)</p>';
