# Filled by add-game on deploy; set manually for local dev.
PORT=4880

# Only needed if server-to-server dg-accounts calls are ever added.
# JWKS token verification (the normal auth path) needs no secret.
DG_APP_SECRET=
